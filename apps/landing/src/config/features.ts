// Switch `docs` back to `true` when the standalone Documentation landing page
// should be public again.
export const WEBSITE_FEATURES = {
  docs: false,
  ecosystemNav: false,
  localePicker: false,
  defaultLocale: 'en',
} as const;
